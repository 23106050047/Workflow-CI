name: diabetes-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "diabetes_prediction_dataset_preprocessing.csv"}
    command: "python modelling.py {data_path}"
